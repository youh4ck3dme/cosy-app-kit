# v0.4.6 — Kernel Observatory Foundation

## Summary

Exports the diagnostics package (`commandTelemetry`, `performanceTracker`, `invariantReporter`, `auditTrail`, `kernelHealth`) from the public Builder Kernel barrel. Hosts may opt in; dispatch semantics are unchanged.

## Evidence

- ADR: [ADR-0006](../adr/ADR-0006-kernel-observatory.md)
- Code: `src/lib/builder/diagnostics/`
- Barrel: `src/lib/builder/index.ts`
- Tests: `src/lib/builder/diagnostics/diagnostics.test.ts`

## Non-goals

Design Canvas UI, marketplace metrics, remote PII analytics.

## Tag

`v0.4.6-kernel-observatory` (cut after merge to `main` when operators need a pinned release).
